<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="Board">
	<insert id="insertBoard">
		insert into consult(title, category, name, nickname, password, content)
		values (#{title}, #{category}, #{name}, #{nickname}, #{password}, #{content})
	</insert>
	<update id="updateBoard">
		update consult
		set title = #{title}, category = ${category}, nickname = #{nickname},
		content=#{content}
		where seq=#{seq}
	</update>
	<delete id="deleteBoard">
		delete from consult where seq=#{seq}
	</delete>
	<select id="getBoard" resultType="board">
		select * from consult where seq = #{seq}
	</select>
	<select id="getBoardList" resultType="board">
		select * from consult order by seq desc
	</select>
</mapper>
